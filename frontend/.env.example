# API base for the web app (Vite). Mobile uses EXPO_PUBLIC_API_URL.
# VITE_API_URL=http://localhost:8000

# Set to true only when you want the browser demo dataset (no backend).
# Leave unset or false so web and the supervisor mobile app read the same live API.
# VITE_USE_MOCK=false
