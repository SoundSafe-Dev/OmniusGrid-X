# Task pool — 2026-09-04

Written 2026-09-04 on `hamad/converged-pre-main` at `faca6e5b`, for the whole team, grouped by
lane so it can be handed out as-is. Every item is independently assignable; reassign freely.

The previous pool (2026-08-08) is archived at
[`task-pool-2026-08-08.md`](task-pool-2026-08-08.md); `next-week-task-pool.md` (also 2026-08-08)
is superseded by this file. Every figure below was re-derived from the repository on
2026-09-04, not carried from either.

> ### ⚠️ Verify before you start
>
> Six weeks of work landed between the last pool and this one — Waves 1–4 of the FS-769→968
> sprint (SLA instrumentation, durability, capacity, backend correctness) plus both dev
> branches (`hridyansh/integration`, `RAG-Compliance-Doc-Pipeline`) merged in. Several old
> entries closed as a side effect of work in an adjacent file; **spend the first ten minutes of
> any item reproducing its claim** before starting it. If it does not reproduce, correct the
> entry in place with the date — that habit is the only thing that has kept these pools from
> rotting in either direction (2026-07-26 rotted long, 2026-08-08's first draft rotted short).

Sizes: **S** under half a day · **M** 1–2 days · **L** 3+ days.

---

## Verified state, so nobody re-derives it

Measured 2026-09-04 on `hamad/converged-pre-main` at `faca6e5b`.

| Fact | Value |
|---|---|
| Backend suite | **5,622 passing**, 105 skipped |
| Frontend suite | **1,225+ passing**; `tsc --noEmit` clean |
| Edge-agent suite | **490 passing** + 109 DDIL scenarios (measured 2026-08-28, at the merge) |
| `response_model` coverage (`test_response_model_coverage_ratchet.py`) | `MAX_UNDECLARED` **46** (52 → 46 this week; the 6 closed were Hamad's, the 46 remaining are HARSH's correlation/MLOps + kanban/analysis_sessions routes) |
| Permissive response models (`test_a_permissive_response_model_is_not_a_contract.py`) | `MAX_PERMISSIVE` **9** (22 → 9 this week; remainder is 3 RAG + 1 correlation + 5 genuinely-dynamic) |
| Swallow surface (`test_the_swallow_surface_only_shrinks.py`) | `MAX_SWALLOWING` **198** · `MIN_COUNTED` **18** (was 199 / 14) |
| Contract gate | run in progress as of this writing — see FS-911 below, do not assume last week's 445/546 still holds without re-checking |
| `main` vs this branch | **659 ahead, 0 behind** — see D1, unchanged in substance since 2026-08-08 |
| Migrations | 82; the 4 non-idempotent ones from FS-578 are unchanged |
| Lane-failure register (`tests/_lane_failures.py`) | non-empty — see the note under HARSH's section on `LANE_ROUTERS` double-duty |
| This pool | 24 tickets across 4 lanes, 2 decisions |

**Both dev branches are merged and historical.** Tell Hridyansh and htreinen to reset onto
`converged-pre-main` if either is still committing to `hridyansh/integration` or
`RAG-Compliance-Doc-Pipeline` — those branches are dead ends now.

---

# Decisions

### D1. Promote `main` · *carried from 2026-07-26 (#3, #39) and 2026-08-08 (D1) — its FOURTH pool*

`hamad/converged-pre-main` is now **659 commits ahead of `main`, 0 behind**. This is not a
harder decision than it was a month ago; it is the same window, now wider. Every day it stays
open is another day new branches fork from a tree that predates the whole convergence and both
merged dev lanes.

**Done when:** the promotion has run, or a date is written down that isn't "next pool."

### D2. Three dormant services — reviewed, deliberately left, revisit if priorities change

`tactical_engine.py`'s command dispatch, `erp_database_replication.py`'s CDC scaffolding, and
`keycloak_service.py`'s SSO read path (FS-913) were each found already sitting in a correct
terminal state — refusing rather than lying — and each needs a decision only outside this
sprint's scope can make: a safety review for autonomous industrial actuation, a Kafka-routing
call for ERP data, and an SSO provisioning spec. Put to Hamad directly and left untouched.

**Done when:** one of the three gets an owner and a real decision, or this entry is re-affirmed
closed at the next pool.

---

# Hamad — platform, contract gate, Wave 5 (testing/chaos/drills)

### FS-911. Raise the contract gate's floor by measurement, not arithmetic · M · *in progress*

`scripts/contract_ratchet.py` holds `BASELINE_WITHOUT_BROKER = BASELINE_WITH_BROKER = 445`
against `EXPECTED_TOTAL = 546`. A throwaway Postgres 15 + Redis (never the dev database, never
`:5432` — this session used `:5434`/`:6380` via plain `docker run`, not the compose stack) was
migrated, `omniusgrid_contract` was provisioned `NOSUPERUSER NOBYPASSRLS`, and
`tests/test_api_contract.py --junitxml` was run against it with no broker. **Whoever picks this
up next: read the junit report this run produced, run `contract_summary.py` and
`contract_ratchet.py` against it, fix anything clearly attributable to this lane, and raise the
floor only from a second confirming run — never from one.** Tear down `contract-gate-pg` and
`contract-gate-redis` when done.

**Done when:** the floor is raised by a measured, reproduced amount, or the run is written up as
having found nothing new and the containers are torn down anyway.

### FS-914–943. Wave 5 — testing, chaos and drills · L

Wave 4 (backend correctness and performance, FS-879–913) is now complete. Next in the fixed
sprint: kill the database mid-write, kill a backend pod mid-request (the `preStop` hook's
acceptance test), partition the broker, take Redis away, fill edge and cloud storage, clock
skew across a send, extend the DDIL outage to multi-day, a sustained load test to establish a
devices-per-cluster figure, a 24h soak, a spike test against the HPA↔pool interaction, and five
timed drills (DR game day, CNPG failover, rollback, backup-corruption, tabletop). See the sprint
plan for the full FS-914→943 list and their explicit acceptance criteria (each produces a
written number).

**Done when:** each drill has run once and produced the number the plan asks for.

### FS-960. No virtualisation anywhere · M

`predictive/Historian.tsx:370` renders 2,500 DOM nodes from a `limit: 5000` query, saved only by
a `slice(0, 500)`. `analytics/AnalyticsPages.tsx:80`, `fleet/FleetPages.tsx:167-179` and
`admin/FleetTargeting.tsx:218` have the same shape. Zero virtualisation libraries installed.

**Done when:** at least Historian's table is windowed and the `slice` safety net is no longer
load-bearing.

### FS-967. Two tiles on the same fleet screen can disagree · S

`fleet/FleetPages.tsx`'s `limit: 500` cap makes `onlineCount` silently wrong past 500 assets
while the "Total Assets" tile uses the real `total`. Both numbers are visible on one screen with
no indication either is capped.

**Done when:** `onlineCount` is derived the same way `total` is, or the tile says it's a sample.

---

# Hridyansh — OTA / fleet lane

### FS-OTA-1. `rollout_orchestrator.py` dispatches `model_update` to a handler that doesn't exist · M · *carried, largest item in the lane*

`rollout_orchestrator.py:297` dispatches `model_update` to every device; the agent answers
`unknown_action`, so **a healthy device is recorded as a failed rollout** — the wiring is about
three lines. `test_dispatched_commands_have_a_handler.py:51` names the gap; `api/health.py:728`
currently states as fact that the agent handles it, which is also wrong and worth fixing in the
same change.

**Done when:** the agent has a real `model_update` handler, or the orchestrator stops claiming
one exists.

### FS-OTA-2. Fleet/RolloutDetail table actions — pause/resume/rollback half-wired · M

`useFleet` has pause/resume, but they're only wired on the detail page, not the fleet table
itself. `rollback_command_id` is displayed but never triggerable from the UI at all. The detail
view renders 1000-row fleets unpaginated with raw JSON in event cells rather than a formatted
column. From `docs/engineering/page-enhancement-review.md:294`.

**Done when:** pause/resume are reachable from the table, rollback is triggerable, and the
detail view paginates.

### FS-OTA-3. `edge-agent`'s idempotency prefix is unprotected · S · *decision, not a bug*

`/api/v1/fleet` and `/api/v1/edge` sit outside the idempotency-key middleware
(`test_the_idempotency_seam_is_declared.py`'s `UNPROTECTED` register) — a retried fleet command
or edge-status write can double-apply. This is now squarely this lane's call since the OTA/fleet
ownership consolidation.

**Done when:** either prefix is wrapped, or the register entry says explicitly why a retry here
is safe to double-apply.

---

# HARSH — MLOps + correlation-AI (kanban, analysis_sessions, engines, nlp/logistics correlation, model_monitoring)

**Lane discipline note:** `tests/_lane_failures.py`'s `LANE_ROUTERS` is a SHARED exemption list
used by at least three unrelated guards for different reasons (realdb endpoint smoke, write-
endpoint-rejects-cleanly, and — incidentally — the offset-ceiling check). Adding a module here
to satisfy one guard silently exempts it from the others too; check what else reads the list
before adding to it.

### FS-887. Two N+1 query patterns in this lane · M · *registered, not edited*

`api/kanban.py:1522-1560`'s `/workload` runs 4 aggregate queries per user — 50 users is 201
queries, collapsible to one `GROUP BY assigned_to` with `FILTER` clauses.
`api/analysis_sessions.py:494-504` runs 2 `SELECT`s per session plus
`len(...scalars().all())` to count, materialising whole rows to get a number.

**Done when:** both collapse to O(1) queries regardless of row/user count.

### FS-908-tail. 46 undeclared response models · L

The general ratchet (`MAX_UNDECLARED = 46`) is now entirely this lane: correlation/MLOps routes
(`engines.py`, `correlation_integration.py`, `logistics_correlation.py`, `nlp_correlation.py`,
`model_monitoring.py`) and kanban/analysis_sessions. Same pattern Hamad's lane just closed on
`geotab.py`/`registries.py`/`yard.py`/`bulk_operations.py`/`auth.py`/`telemetry.py`: read the
service function's actual return shape and declare a `BaseModel` for it.

**Done when:** `MAX_UNDECLARED` is lowered by however many get declared, mutation-tested by
stashing the change and confirming the ratchet fails against unmodified source.

### FS-909-tail. 1 permissive response model + 5 registered-dynamic · S

`logistics_correlation.py:97`'s `GET /dock-production-sync` answers `Dict[str, Any]`. The 5
`DELIBERATELY_DYNAMIC` entries (feature-flags ×3, simulation ×2) are correctly registered as
genuinely dynamic and do not need a schema — only the one undeclared route does.

**Done when:** `dock-production-sync` gets a real schema and `MAX_PERMISSIVE` drops by one.

### FS-UNREAD-1..3. Three declared body fields nothing reads · M

`analysis_sessions:POST /{session_id}/correlate` never reads `auto_integrate`.
`nlp_correlation:POST /intake/cross-correlate` never reads `auto_integrate` either — same field,
two routes. `logistics_correlation:POST /load-quality` never reads eight declared fields
(`claim_amount`, `claim_filed`, `manufacturing_correlation_score`, `metadata`, `resolved_at`,
`root_cause_asset`, `root_cause_operation`, `trailer_id`) — the one metadata-vanishes route left
unwired after FS-669 closed the other six of that shape. Each needs the FS-907 treatment: decide
per field whether it's schema-side (remove from Create) or genuine data loss (wire it through).

**Done when:** `test_declared_body_fields_reach_the_service.py`'s `UNREAD` register loses these
three entries.

### FS-SPLIT-1..3. Three split-input routes · M

`POST /engines/tactical/infer` reads `asset_id` from the query string and `feature_vector` from
the body — a client posting one JSON document scores against the default asset. `POST
/nlp/correlation/chat` reads `message` from the query and `conversation_history` from the body —
a natural call sends an empty message. `POST /nlp/sessions/{id}/data/upload` reads `data_type`
from the query beside a multipart file — omitting it silently applies a default type. Same
FastAPI parameter-source defect FS-902 just closed for five routes in Hamad's lane: collapse
each to one Pydantic body model (multipart stays multipart, just add the field to the form).

**Done when:** `test_no_route_splits_its_input.py`'s `SPLIT_INPUT` register loses these three.

### FS-ENGINES-1. Engines pages: dead signals and no client for two routes · M

`X-Engine-Not-Running` is set by the backend but read by no frontend code, so a dead engine loop
renders as healthy. Strategic's history route (`engines.py:202`) has no client in `api/engines.ts`
at all. MLOps deploy/rollback has no confirmation dialog before an irreversible action.
`model_monitoring.py`'s drift/performance endpoints have no frontend client whatsoever. From
`docs/engineering/page-enhancement-review.md:199-207`.

**Done when:** the health header is surfaced, Strategic history has a client, deploy/rollback is
confirmed, and model-monitoring has at least a read-only view.

### FS-KANBAN-1. Kanban ships backend surface the frontend never uses · L

Comments, timer start/stop, time-logs, workload view and rules UI all exist as routes with no
frontend consumer, and production code still branches on `USE_MOCK`. From
`docs/engineering/page-enhancement-review.md:214-218`.

**Done when:** each backend surface either gets a frontend consumer or is explicitly deferred
with a reason, and the `USE_MOCK` branches are gone from production code paths.

### FS-NLP-1. CorrelationAIPane: sessions vanish, transcript caps silently · S

No rename/archive/delete for sessions — an ended session just disappears from the list.
Transcript caps at 100 messages with no load-older affordance. Auto-integrate outcomes are
computed but never reported to the user. From `page-enhancement-review.md:245-247`.

**Done when:** sessions are manageable and the transcript can page past 100.

### FS-OTHER-1. `rag-inference/Dockerfile` uses an unpinned, out-of-date base image · S

`FROM python:3.10-slim`, no digest, a Python version behind the rest of the platform. Registered
as MLOps lane in `OTHER_LANE_DOCKERFILES` — mechanical fix, no product decision attached.

**Done when:** the base image is digest-pinned and current.

---

# htreinen — RAG (ingestion, retrieval, vector store, compliance assistant)

### FS-RAG-1. Three permissive response models · S

`rag.py:331` (`list_documents`), `rag.py:437` (`delete_document`), `rag.py:455` (`health`) all
answer `Dict[str, Any]`. Same mechanical fix as the rest of `MAX_PERMISSIVE`'s burn-down: declare
the real shape each handler builds.

**Done when:** all three declare a real `BaseModel` and `MAX_PERMISSIVE` drops by three.

### FS-RAG-2. `/api/v1/rag` has no idempotency protection · S · *decision*

A retried ingest or delete can double-apply. `UNPROTECTED` register,
`test_the_idempotency_seam_is_declared.py:90`.

**Done when:** the prefix is wrapped, or the register says explicitly why a retry is safe here.

### FS-RAG-3. Six items from the RAG k8s status doc · L · *carried from `docs/rag_status_8.27.md`*

(1) k8s-cluster deployment has never been verified — only the compose/native-process path is
proven. (2) mTLS env-var mismatch: `base/backend-deployment.yaml` sets `CA_CERT_PATH` etc. while
the code reads `MTLS_CA_CERT_PATH` etc., and `backend-tls`'s fate is undecided. (3) Namespace
topology is an open decision — keep `omniusgrid-rag` standalone or fold into
`overlays/{staging,production}` — and it blocks (4) and (5). (4) `rag-inference`'s built image
tag isn't wired into whichever kustomization owns `base/rag/`. (5) `QDRANT_API_KEY` /
`RAG_INFERENCE_API_KEY` + NetworkPolicies are blocked on (3). (6) Qdrant's PVC storage class
isn't pinned per environment.

**Done when:** (3) is decided (that unblocks 4 and 5), and 1/2/6 are each closed or re-scoped
with a date.

### FS-RAG-4. ComplianceAssistant doesn't surface the document library it already has · M

`rag.py` exposes `/documents` list, `/ingest`, delete and `/health` — none of it is in the UI, so
"nothing matches" is indistinguishable from "the corpus never loaded." No export of an
answer-plus-citations for an audit trail. No corpus/framework scoping filter. **Explicitly
marked "record, don't ship without authorisation"** in the source review — this is a compliance
surface, treat the UI work with the same care as the backend.

**Done when:** the library is visible, answers can be exported with citations, and scoping
exists — with sign-off before shipping, per the note above.

---

# Alex — intake

No open registered findings this pool. The two mechanical `IntakeInbox` bugs from the last
review (dead "View Results" button, non-reactive `statusFilter`) are recorded as **fixed** in
`docs/engineering/page-enhancement-review.md`'s "Honesty debt paid" section. Two items carried
from other lanes still name Alex's files and are worth a look when picked up:

### FS-ALEX-1. `IntakeInbox.tsx` has zero memoisation across 2,013 lines · M

~46 `useState` hooks, zero `useMemo`/`useCallback` anywhere in the file — the largest
unmemoised surface in the frontend (registered from Hamad's lane, FS-966).

**Done when:** the obvious re-render hotspots (list filtering, per-row callbacks) are memoised.

### FS-ALEX-2. Two hand-rolled mutation surfaces bypass the toast-on-success sweep · S

`IntakeInbox.tsx:591,622` don't use `useMutation`, so the existing success-toast guard can't see
them (registered from Hamad's lane, FS-947).

**Done when:** both call sites either move to `useMutation` or get an equivalent visible
success/failure signal.

---

## Notes for redistribution

* **Lane discipline holds.** `kanban.py`, `analysis_sessions.py`, `nlp_correlation.py`,
  `logistics_correlation.py`, `model_monitoring.py`, `engines.py` are HARSH's; `rag_*.py` and
  `rag.py` are htreinen's; fleet/OTA orchestration is Hridyansh's; `IntakeInbox.tsx` is Alex's.
  Mechanical fixes (a `response_model`, a split-input collapse, a Dockerfile pin) are fine from
  any contributor; product and design decisions — session management, provisioning semantics,
  what ships to a compliance surface — are not.
* **Every item names its evidence** — a file, a line, a test, or a measurement taken 2026-09-04.
  If a claim does not reproduce, correct the entry in place with the date, the way FS-816 and
  FS-890 were corrected mid-sprint. That is worth more to the next pool than the ticket was.
  Three of the last four pools rotted this way at least once.
* **Ratchets only shrink, floors only rise.** `MAX_UNDECLARED`, `MAX_PERMISSIVE`,
  `MAX_SWALLOWING` and the contract gate's baselines all moved this week purely by measurement —
  never by raising a number to make a build pass. Keep that discipline; it's the only thing that
  keeps these pools honest instruments rather than aspirational lists.
* **Both merged dev branches (`hridyansh/integration`, `RAG-Compliance-Doc-Pipeline`) are
  historical.** Reset local work onto `converged-pre-main` rather than continuing on either.
