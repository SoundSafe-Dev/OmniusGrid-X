# Edge agent env template (task 17). Copy to edge-agent/.env.
REDPANDA_URL=localhost:9092
ORGANIZATION_ID=dev-org
AGENT_ID=agent-001
BUFFER_RETENTION_HOURS=24
METRICS_PORT=9108

# Cloud enrollment (obtain a one-time bootstrap token from the backend operator)
CLOUD_URL=https://localhost:8000
EDGE_BOOTSTRAP_TOKEN=
