paho-mqtt==1.6.1
aiokafka==0.9.0
aiosqlite==0.19.0
pydantic==2.5.0
pydantic-settings==2.1.0
structlog==23.2.0
numpy<2
opencv-python==4.8.1.78
pytesseract==0.3.10
Pillow==10.1.0
watchdog==3.0.0
PyYAML==6.0.1
pyserial==3.5
asyncua==1.1.5
pymodbus==3.6.1
httpx==0.25.2
python-dotenv==1.0.0
prometheus-client==0.19.0
cryptography==46.0.6   # agent identity / CSR / mTLS (edge transport security)

# Optional industrial-protocol driver libraries.
# Each backs one collector; install only those needed for the deployed hardware.
# The collectors import these lazily and degrade gracefully if absent.
pylogix==1.0.5          # EtherNet/IP (Rockwell / Allen-Bradley)
python-snap7==1.3       # PROFINET / S7 (Siemens)
BAC0==2023.6.9          # BACnet (building automation) — wraps bacpypes
python-can==4.3.1       # CAN bus (vehicle / machine controllers)
pysnmp==4.4.12          # SNMP (network devices / gateways / UPS)
tahu==1.0.9             # MQTT Sparkplug B protobuf schema (+ paho-mqtt above)
dnp3-python==0.2.3b3    # DNP3 (utility / SCADA outstations)
sounddevice==0.5.1      # audio capture for the audio collector ("device" source;
                        # "simulate" demo mode needs only numpy, provided by opencv)
